# CPU-only torch (Raspberry Pi 4B har ingen GPU). Utan denna extra-index
# kan pip på aarch64 av misstag installera en CUDA-byggd wheel (t.ex.
# torch+cuXXX), som kraschar med SIGILL i nn.LSTM på Cortex-A72 - en sådan
# build är inte ARM-optimerad och antar instruktioner CPU:n saknar.
--extra-index-url https://download.pytorch.org/whl/cpu

yfinance>=0.2.36
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
lightgbm>=4.1.0
torch==2.12.1+cpu
optuna>=3.4.0
ta>=0.11.0
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.66.0
joblib>=1.3.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Alt-data-experiment (altdata/): hämta MFN-pressmeddelanden + LLM-sentiment.
# requests följer normalt med yfinance men listas explicit. anthropic behövs
# bara för sentiment.py (poängsättningen) – resten av pipelinen funkar utan.
requests>=2.31.0
anthropic>=0.40.0
